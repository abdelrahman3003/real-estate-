<odoo>
    <record id="purchase_order_tree_inherit" model="ir.ui.view">
        <field name="name">purchase.order.tree.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="decoration-danger">purchase_priority == 'high'</attribute>
            </xpath>

            <xpath expr="//field[@name='partner_ref']" position="after">
                <field name="purchase_priority" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <header>
                <button name="duplicate_purchase_order" type="object" class="btn-primary" string="Duplicate"/>
                <button name="action_done" type="object" class="btn-primary" string="Done"/>
                <button name="approve" type="object" class="btn-primary" groups="app_one.property_manager_group"
                        string="Approve" invisible="state !='to approve'"/>
            </header>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="open_view_order_lines" type="object" class="oe_stat_button" icon="fa-list">
                    <field name="line_count" widget="statinfo" string="Order Lines"/>
                </button>
            </xpath>

            <xpath expr="//field[@name='partner_ref']" position="after">
                <field name="expected_delivery_notes"/>
                <field name="purchase_priority"/>
                <field name="total_quantity" readonly="1"/>
            </xpath>

        </field>
    </record>

</odoo>